{
  "name": "covenant-sync-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Optional Postgres sync API for The Covenant Wedding Planner (offline-first client remains default).",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "db:init": "node scripts/init-db.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.3"
  }
}
